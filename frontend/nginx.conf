server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA：所有路由 fallback 回 index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
